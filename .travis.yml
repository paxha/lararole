language: php

cache:
  directories:
    - $HOME/.composer/cache/files

php:
  - 7.2
  - 7.3
  - 7.4snapshot

matrix:
  include:
    - php: 7.2
  allow_failures:
    - php: 7.4snapshot

before_script:
  - travis_retry composer self-update

script:
  - composer test